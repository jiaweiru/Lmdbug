// Simple Protocol Buffer definition for LMDB example
// Minimal message with text and audio fields

syntax = "proto3";

package simple;

// Simple message containing text and audio data
message SimpleMessage {
  // Text content (string)
  string text = 1;
  
  // Audio data as bytes (24kHz 16-bit PCM, stored as raw bytes)
  bytes wav = 2;
}